Scan-to-Editable-Plan V1 — acceptance-completion continuation

DEVELOPMENT MODE: FAST FEATURE

Continue the existing mandate in the existing feature worktree. This is a focused continuation of real-input recognition, reproducible installation and Illustrator acceptance, not a restart, a new architecture project or authorization to integrate.

The outcome remains: a consumer imports a real apartment scan, reviews/confirms its scale and uncertain details, edits actual walls/openings in 2D, sees matching 3D, furnishes, and exports independently editable PDF artwork. Preserve the reported completed editor, topology, persistence and export work.

1. Resume safely and resolve the source-identity discrepancy

Read applicable AGENTS.md, the existing MANDATE.md, FEATURE-STATUS.md and ILLUSTRATOR-ACCEPTANCE.md. Inspect actual HEAD, status and running processes before changes.

Existing feature worktree:
/Users/justus/Developer/interior-ai-scan-to-editable-plan

Branch: feature/scan-to-editable-plan-v1

Reported tested candidate: dbc0ef4013e9fe085c16a7baa3bfb11736706af3

Reported tested tree: fb3b36a9fbe0c9c9debf82fa9c7d575370411584

Reported subsequent status-only commit: 21b3354. Verify its full identity and actual diff; do not assume current HEAD or that it was tested. Record any further changes.

Preserve the existing immutable preview and private evidence. Do not modify the dirty primary checkout, shared node_modules, other worktrees, or existing test artifacts. Verify live ports/PIDs rather than reusing stale process numbers.

An independent check of the original file attached in ChatGPT, IMG_4900(1).jpg, found:

Dimensions: 836 × 1080 pixels.

Bytes: 93,802.

SHA-256: a0d0209a591dc57352b803fc5f8678055da8a8a24a5c2bd5d795ed8dff2d5006.

That exactly matches the SHA-256 recorded in your handoff for /Users/justus/Downloads/IMG_4900.jpg. Rehash that local file. If it still matches, close the source-identity discrepancy: it is the same input by byte hash despite the filename suffix. Preserve the historical record and append the correction. Do not keep requesting a different original or describe this source as unconfirmed after the match.

This confirms input identity only, not extraction accuracy or the current file contents on the Mac before rechecking.

2. Prove a normal, isolated dependency installation

The previous build/tests used existing packages plus an isolated fontkit overlay. Close this local reproducibility gap without turning it into repository-wide certification.

Create a disposable source copy at an explicitly recorded revision, with its own real node_modules directory and no links into other worktrees. Include required tracked/LFS assets using the established safe procedure. Use the repository-declared Node/npm versions, package manifest, lockfile and project configuration; do not update runtimes or dependencies merely to get a pass.

Run the normal lockfile-based clean installation, normally npm ci, in that disposable directory only. Respect existing network, lifecycle-script and tooling permissions. Do not install through shared symlinks, use an undeclared package overlay, or bypass permission/security controls.

Verify that @pdf-lib/fontkit and all feature dependencies/assets resolve through the declared installation. Record dependency versions and lockfile identity, and confirm the install did not rewrite the manifest/lockfile.

Run the production build, type checks, feature/domain checks and an actual PDF/SVG export smoke from this installation. If a genuine feature dependency/lockfile defect appears, fix it in the feature branch, checkpoint normally and repeat the affected clean-install checks on the new exact revision. No broad dependency upgrades or unrelated repairs.

An unavailable registry, missing authorized asset or permission denial is an environment/approval blocker, not a successful clean install. Continue independent work while it is pending.

3. Diagnose the real scan before blaming image quality

The handoff reports 120 strokes and 61 OCR reference annotations, but zero inferred topology and unresolved scale. That does not yet demonstrate the requested scan-to-editable-wall journey.

Instrument the actual pipeline and identify why usable topology is absent. Distinguish geometry not detected, geometry detected but rejected, topology construction not reached because scale fails, and UI/promotion rules discarding otherwise useful candidates.

For every scale conflict, show the source crop, original/processed coordinate transforms, text candidates, chosen number/units, dimension orientation, extension endpoints and residual. Check coordinate-space mismatches, incorrect endpoint association, rotated OCR, projected versus Euclidean lengths, and source distortion/inconsistent printed dimensions. These are hypotheses to test, not predetermined diagnoses.

Do not conclude that a better scan is the only solution from aggregate residuals alone. Do not increase the frozen error threshold, suppress conflicting evidence without explanation, silently warp the image, or select whichever reading makes the solver pass.

Separate source-pixel candidate recovery from confirmed metric geometry where the current architecture allows it. Preserve useful uncalibrated wall/opening proposals in review without pretending they are a confirmed-scale canonical plan. A blocked scale step must not silently discard recoverable evidence. Keep authoritative geometry/3D/publication gates intact.

Implement only necessary, source-supported fixes. Do not add a separate editor or geometry engine, hardcode this apartment, populate the import with an authored JSON plan, or call reference strokes editable semantic walls.

4. Demonstrate an honest recovery route through the consumer UI

Use the hash-confirmed real scan to exercise the production consumer workflow. Resolve scale through independently verified source dimension endpoints and explicit consumer corrections where warranted. Where a drawing is genuinely inconsistent, present the conflict and a bounded recovery choice, not a falsely verified import.

A consumer-reviewed reconstruction based on chosen reliable dimensions must be a visibly user-authored/measured proposal, distinct from exact source recovery. Record which constraints were used, excluded or unresolved and why. Ambiguous geometry must remain visibly uncertain. Request additional measurements or a better source only when the existing evidence cannot support the required decision.

Demonstrate import, review, wall removal/addition, an opening edit, save/reload, matching 3D, furnishing and PDF export from the real-image-derived plan. Record all human interventions and source-derived versus authored geometry. Preserve original source/reference revisions and privacy protections.

When safe completion remains impossible from this source, report precisely what additional information is needed and demonstrate the source's truthful draft/recovery state. Then demonstrate successful assisted conversion on another independently checked supported real scan. Do not require fabrication just to force success on every input, and do not silently drop the original scan from the results.

Report separately: automatic recovery before corrections, assisted outcome after corrections, and the existing authored-fixture editor/export results. Manual tracing and scripted JSON insertion do not demonstrate automatic recognition. Use actual human observations for correction-time claims; label scripted interaction timing as scripted.

5. Test recognition on independent real plans

Reuse only authorized private plans or repository fixtures with adequate processing/benchmark rights. Keep client plans and ground truth out of public commits and telemetry. Never publish private inputs as convenience fixtures.

Build a compact independent source inventory of walls, openings, dimensions and uncertain areas. Do not derive expected truth from the same extraction output being tested. Mark machine-produced reference annotations as unreviewed until independently checked.

Request one targeted batch of additional authorized apartment plans directly in Codex if required. Start with a small varied batch for debugging, but do not claim that it satisfies the original 15–30-plan pilot. Keep independent holdout layouts; rotated/resized variants of one plan are not independent layouts.

Record source completeness, missing/extra critical elements, geometric residuals, assistance needed and unsupported cases. Keep incomplete broader coverage explicit rather than inventing accuracy percentages or human time savings.

If the existing optional AI classifier is the next justified experiment, request bounded approval inside Codex early rather than endlessly tweaking local thresholds. Specify provider/model, images transmitted, data treatment, maximum calls, maximum spend and success criteria. Existing credentials do not grant authorization. Do not run paid/external recognition, create infrastructure or change secrets without approval. Continue local work while awaiting it.

6. Finish native Illustrator acceptance without an automation loop

Use the existing authored PDF/SVG test pack first to isolate exporter behavior from recognition. Add a real-scan-derived sample after supported recovery succeeds.

Open copies as documents in Illustrator 2024, not as linked placed pages. Test selecting/moving/deleting one wall, editing a window, separating/manipulating a door leaf and swing, moving a fixture, editing a dimension number, then saving a copy, closing and reopening. Check scale and font substitution explicitly. Confirm that surviving artwork remains separately editable after reopening.

Record exact app version, input/output filenames and hashes, observed actions and problems. A visually correct PDF, separate path/text operators or success in another editor does not establish native Illustrator acceptance.

The handoff records two AppleEvent timeouts. Do not repeatedly retry that route without a specific new diagnosis. When authorized application control is unavailable, ask the user directly in Codex to perform the compact manual checklist and return results. Do not alter macOS security permissions, close unrelated unsaved documents or overwrite original files.

Use ILLUSTRATOR_ACCEPTANCE_PENDING until actual results arrive. Continue all independent local work. Fix actual exporter defects within scope and retest affected outputs, not the entire project.

7. Retain scope and accurate status

The floor-wide prohibition on adding/joining partitions when any curved wall is present is broader than unsupported curved-wall authoring. Keep this limitation explicit in the UI, supported-input description and acceptance report. Do not silently advertise unrestricted edits on such floors. Do not expand this continuation into a new curved-topology engine; any broader scope decision belongs with the user in Codex.

Retain the inherited renderer-resource growth finding and its unresolved attribution. Do not call memory stability PASS or begin a broad renderer rewrite. Reclassify only if new evidence shows this feature worsens it or it prevents the ordinary required workflow; report and address direct feature regressions without hiding inherited behavior.

Use the original A/B/C/D failure classes, required checks and frozen tolerances. Preserve regression coverage for the three reviewed fixes. Run affected tests during development and an exact-candidate verification set at the end. Do not reset the original review count or begin another repository-wide audit. Any targeted review must focus on new changes and remaining acceptance boundaries.

Continue local implementation/testing/ordinary commits under the original mandate. No push, PR, merge/rebase/amend, deployment, shared database writes, secrets changes or external recognition without the required separate authorization. Re-read the live target only when later authorized integration is actually being prepared; do not merge target drift into this work unasked.

Keep FEATURE-STATUS.md concise and current: source identity, clean-install result, real-input result, Illustrator result, exact candidate and next action. Preserve old evidence; do not relabel earlier runs as testing new source.

Do not describe all remaining work as exclusively external while local recognition diagnosis or reproducible installation remains open. Use IN_PROGRESS or BLOCKED_LOCAL for active local work, AWAITING_APPROVAL_IN_CODEX for a genuine permission boundary, and IMPLEMENTED_PENDING_EXTERNAL_ACCEPTANCE only with a precise outstanding list. READY_FOR_FEATURE_REVIEW requires the original required acceptance conditions, not a reduced after-the-fact target. Merge/deployment readiness remains separate.

The final checkpoint must answer: What works automatically on real scans? What requires consumer correction? What still fails? Did the normal clean installation pass? Did the PDF edit/save/reopen test pass in Illustrator? Which exact revision was tested?

Start by rehashing the source and closing that discrepancy, then perform clean-install verification and real-scan diagnosis. Continue through these bounded tasks without another ChatGPT prompt. Ask for genuine external or human inputs directly in Codex and continue independent work while waiting.