# Cart CI diagnostics — local handoff

Status: **CART_DIAGNOSTICS_PREPARED_REVIEWED_LOCAL_VALIDATION_INCOMPLETE**. Preparation is local; publication and CI are not authorized. The pinned actionlint check remains unavailable, so the fully validated completion status is not claimed. Separate review confirmation and exact byte bindings are retained alongside the patch under `test-results/cart-ci-diagnostics-preparation/`.

Baseline commit: `68110d2cc6c907cf9d12ee2ebe2be72691860af8`; tree: `bc9960c805df6f52cb884b399c27601f1560e489`. Local branch: `fix/cart-ci-failure-diagnostics`, worktree `/Users/justus/Developer/interior-ai-cart-ci-diagnostics`. No new commit exists. Recorded integration target: `a4295bb60c2819c0c3d380f2c758aad8f20f21db`; this batch makes no fresh remote-state claim.

The complete four-path patch is `.github/workflows/ci.yml`, `tests/e2e/cart-diagnostics-upload.mjs`, `tests/e2e/cart-diagnostics-upload.test.mjs`, and this document. Shared sanitizer/audit/validator contracts, required manifest and commands are unchanged. The new standalone Node synthetic test is not registered as a new required gate; it was run explicitly. No registration change is necessary for the collector to run after the existing Cart gate. Existing dependencies are reused through a local ignored symlink; no installation occurred.

## Collection contract

Inputs are `.local/required-test-evidence/ci.cart-overlay-accessibility/{playwright,evidence}.json` and only report-linked `playwright-output/<single-safe-directory>/error-context.md` within that Cart root. The existing producer canonicalizes report paths and validates its gate; it preserves ordinary failed evidence but removes unsafe/unparseable report/output. No survival or historical artifact is assumed.

The helper validates source commit/tree, schema, Cart gate, canonical command, report hash, recorded identities and statistics, zero-retry/full-matrix requirements, freshness and process/report timing. Only normal test-failure issues are eligible; other validation issues reject collection. Existing producers lack workflow run/attempt bindings. Present bindings are checked; inventory run/attempt identifies the collector context, not independently proved producer provenance.

Symlink, traversal, foreign-root, special, multiply linked, oversized and stale inputs are rejected. Limits are 2 MiB/file, 10,000 JSON nodes, depth 30 and 16 linked contexts; legitimate larger reports can produce no payload. Fresh owned scratch is audited and atomically staged at `.local/cart-diagnostics-upload/`; existing staging is refused. Only newly created synthetic/inspection scratch is removed. Original reports are unchanged.

Portable diagnostic JSON projections retain validated case/browser/outcome and existing result status/duration/retry/start time, numeric source locations and available step durations. They are explicitly noncanonical. Arbitrary errors, call logs, stack/snippet/value text, step titles, streams, inline/other attachments and unknown fields are omitted. Only the exact generic 30-second timeout text is permitted. Context content must pass shared sanitization/audit and a tiny static Cart vocabulary; typical full /design snapshots are omitted. Context inventory preserves validated test/browser/retry attribution. Original and published-copy hashes distinguish transformations. These copies cannot replace required gate evidence or certify absence of all sensitive content by regex alone.

Cart receives an identifier without a command change. Preparation requires `failure() && steps.cart-overlay.outcome == 'failure'`. Upload additionally requires successful preparation and `has_payload == 'true'`. It uses existing `actions/upload-artifact@v4`, repository-accessible artifact `cart-overlay-diagnostics-${{ github.run_id }}-${{ github.run_attempt }}`, 14-day retention, and only the staged directory. No permissions/secrets change. No-payload collection is reported separately from the authoritative Cart failure. Preparation/upload errors cannot turn Cart green or release later matrices. Runner loss/forced termination may prevent collection entirely.

## Evidence and limits

The 29 synthetic filesystem/report cases and workflow condition/structure checks passed. Coverage includes valid failures, two-browser context attribution, malformed and out-of-envelope timing, source/schema/hash mismatch, missing/unsafe/stale input, sensitive strings, attachments and preparation/upload conditions. These are synthetic checks, not Ubuntu Cart reproductions or GitHub Actions execution. Both files pass Node syntax and focused ESLint with zero warnings. Repository truthfulness passes with 27 gates/413 classified sources. `git diff --check` passes. Commands and outputs are retained in the ignored validation record.

Required actionlint **1.7.12** was not available on PATH or bounded standard locations. The repository validator would download its pinned binary; installation/acquisition was excluded. No download occurred. YAML parsing and explicit condition tests passed but do not substitute for actionlint. GitHub upload behavior remains unexecuted.

One separate reviewer found missing report-end validation and lost context attribution. Both were addressed in one remediation cycle with synthetic coverage; the separate confirmation is retained in `review.json`. This is local review, not a submitted GitHub approval. Existing JSON has no step starts and cannot identify the historical stalled operation from a declaration line. A future payload may contain only durations/status plus the same generic timeout; no additional instrumentation was implemented.

Historical run 34746027228 attempts 1 and 2 remain failed and Cart causation remains **UNRESOLVED**. Later Retailer Confirmation, Guest Save, My Designs and Floor Plan Upload matrices were skipped, not passed. Prior local production/development timing passes remain limited observations. Both repairs, historical evidence and PRs #31/#32 are unchanged. Durable requests remain **DEFERRED_BY_OWNER**. PR #32 remains blocked.

## One proposed decision — not executed

Approve temporary checksum-verified acquisition/execution of the repository-pinned actionlint 1.7.12 validator, then, only if it passes without any patch change, commit the exact retained four-path patch atop the bound baseline and publish it by ordinary fast-forward of the existing PR #32 branch. This preserves one maintenance PR and does not change #31. Require PR #32 still open/unmerged at the baseline and the named target unchanged; stop on drift/collision or any failed check. Bind authorization to `reviewed.patch` and `changed-files.json` hashes; any repair or changed bytes require a new decision.

That future publication would authorize existing automatic required PR CI/disposable resources plus the narrowly scoped repository-visible diagnostic artifact above, under retained automation limits. Refresh only material automation facts; deploy-hook/external-caller unknowns stay unknown and newly identified unapproved effects stop publication. Do not manually deploy. Verify the newly created commit/tree, PR head/target, event/merge context, actual workflow revision and checkout so CI exercises these new workflow/helper bytes. Rerunning historical run 34746027228 cannot test this patch. Read all required matrix/check results; no skipped/pending result is PASS. Any subsequent rerun, repair, merge, PR #31 closure or deployment still needs separate authorization.
