# Python side of the floor-plan vectorizer evidence provider (lib/floor-plan-imports/vectorizer-evidence.ts).
# Also needs the Tesseract OCR program (5.x, English data) on the worker: `apt-get install tesseract-ocr` / `brew install tesseract`.
numpy>=1.26
# OpenCV 5 changed the shape of several results (HoughLinesP, contours); built and measured on 4.13.
opencv-python-headless>=4.9,<5
scikit-image>=0.22
pytesseract>=0.3.10
