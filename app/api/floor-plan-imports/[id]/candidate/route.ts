// Public contract alias. The parent handler owns all authorization, optimistic
// version checks, provenance sanitisation, and canonical compilation.
export const runtime = "nodejs";
export { PATCH } from "../route";
