{
  "buildCommand": "npx prisma generate && npm run build",
  "devCommand": "npm run dev -- --webpack",
  "installCommand": "npm ci",
  "framework": "nextjs",
  "regions": ["sfo1"],
  "env": {
    "APP_ENV": "staging"
  },
  "build": {
    "env": {
      "APP_ENV": "staging"
    }
  }
}
